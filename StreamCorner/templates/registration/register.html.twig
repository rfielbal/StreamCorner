{% extends 'base.html.twig' %}

{% block title %}StreamCorner | Inscription{% endblock %}
{% block body_class %}compact-shell{% endblock %}
{% block header %}{% endblock %}
{% block footer %}{% endblock %}

{% block body %}
  <main class="auth-page">
    <div class="container auth-layout">
      <section class="auth-brand stack-lg">
        <div class="stack-md">
          <div class="eyebrow eyebrow-bright">Nouveau client</div>
          <h1>Stream<br>Corner</h1>
          <p>
            Créez votre compte pour commander plus vite, suivre vos achats et retrouver vos produits favoris.
          </p>
        </div>

        <div class="stat-grid">
          <article class="stat-card panel">
            <div class="eyebrow">Commande</div>
            <h2 class="product-name">Paiement plus rapide</h2>
          </article>
          <article class="stat-card panel">
            <div class="eyebrow">Après achat</div>
            <h2 class="product-name">Suivi simplifié</h2>
          </article>
        </div>
      </section>

      <section class="auth-stack">
        <article class="auth-card glass-panel">
          <div class="stack-md">
            <div>
              <div class="eyebrow eyebrow-bright">Inscription</div>
              <h2 class="product-name">Créer un compte</h2>
              <p class="muted">Renseignez vos informations pour préparer vos futures commandes.</p>
            </div>

            {% for message in app.flashes('success') %}
              <div class="requirements-panel glass-panel auth-feedback is-success">
                <span class="material-symbols-outlined">check_circle</span>
                <p class="muted">{{ message }}</p>
              </div>
            {% endfor %}

            {% for flash_error in app.flashes('verify_email_error') %}
              <div class="requirements-panel glass-panel auth-feedback is-danger">
                <span class="material-symbols-outlined">error</span>
                <p class="muted">{{ flash_error }}</p>
              </div>
            {% endfor %}

            <div class="form-error-list">
              {{ form_errors(registrationForm) }}
            </div>

            {{ form_start(registrationForm, { attr: { class: 'stack-md' } }) }}
              <div class="field-grid auth-field-grid">
                <div class="field">
                  {{ form_label(registrationForm.prenom, 'Prénom') }}
                  <div class="input-icon">
                    <span class="material-symbols-outlined">badge</span>
                    {{ form_widget(registrationForm.prenom, { attr: { placeholder: 'Raphaël' } }) }}
                  </div>
                  {{ form_errors(registrationForm.prenom) }}
                </div>

                <div class="field">
                  {{ form_label(registrationForm.nom, 'Nom') }}
                  <div class="input-icon">
                    <span class="material-symbols-outlined">id_card</span>
                    {{ form_widget(registrationForm.nom, { attr: { placeholder: 'Dupont' } }) }}
                  </div>
                  {{ form_errors(registrationForm.nom) }}
                </div>

                <div class="field full">
                  {{ form_label(registrationForm.email, 'Email') }}
                  <div class="input-icon">
                    <span class="material-symbols-outlined">alternate_email</span>
                    {{ form_widget(registrationForm.email, { attr: { placeholder: 'vous@email.fr' } }) }}
                  </div>
                  {{ form_errors(registrationForm.email) }}
                </div>

                <div class="field full">
                  {{ form_label(registrationForm.plainPassword, 'Mot de passe') }}
                  <div class="input-icon">
                    <span class="material-symbols-outlined">lock</span>
                    {{ form_widget(registrationForm.plainPassword, { attr: { placeholder: 'Minimum 6 caractères' } }) }}
                  </div>
                  {{ form_errors(registrationForm.plainPassword) }}
                </div>
              </div>

              <label class="checkbox-line">
                {{ form_widget(registrationForm.agreeTerms) }}
                <span>J'accepte les conditions d'utilisation de StreamCorner.</span>
              </label>
              {{ form_errors(registrationForm.agreeTerms) }}

              <button type="submit" class="btn btn-primary">
                Créer le compte
                <span class="material-symbols-outlined">arrow_forward</span>
              </button>
            {{ form_end(registrationForm) }}

            <div class="auth-meta-line">
              <span class="muted">Déjà inscrit ?</span>
              <a class="eyebrow eyebrow-bright" href="{{ path('app_login') }}">Connexion</a>
            </div>
          </div>
        </article>

        <article class="auth-card glass-panel">
          <div class="stack-md">
            <div class="auth-meta-line">
              <div>
                <div class="eyebrow eyebrow-bright">Avantages client</div>
                <h2 class="product-name">Une boutique plus simple</h2>
              </div>
              <span class="material-symbols-outlined">shopping_bag</span>
            </div>

            <div class="auth-benefit-list">
              <div class="auth-benefit-row">
                <span class="material-symbols-outlined">shopping_cart</span>
                <p>Retrouvez votre panier à chaque connexion.</p>
              </div>
              <div class="auth-benefit-row">
                <span class="material-symbols-outlined">local_shipping</span>
                <p>Suivez vos commandes depuis votre espace client.</p>
              </div>
              <div class="auth-benefit-row">
                <span class="material-symbols-outlined">favorite</span>
                <p>Gardez vos accessoires préférés en favoris.</p>
              </div>
            </div>

            <a class="btn btn-secondary" href="{{ path('app_accueil') }}">
              <span class="material-symbols-outlined">storefront</span>
              Retour boutique
            </a>
          </div>
        </article>

        <div class="footer-copy">(c) 2026 StreamCorner. Tous droits réservés.</div>
      </section>
    </div>

    <div class="auth-watermark" aria-hidden="true">SIGNUP</div>
  </main>
{% endblock %}
